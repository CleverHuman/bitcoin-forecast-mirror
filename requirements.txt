# Local run: data from Databricks
databricks-sql-connector[pyarrow]>=3.0.0
python-dotenv>=1.0.0
pandas>=2.0.0
scipy>=1.10.0
prophet>=1.1.0
matplotlib>=3.7.0
plotly>=5.0.0  # optional; Prophet uses it for interactive plots (suppresses "Importing plotly failed")

# Live / paper trading dependencies
python-binance>=1.0.19      # Binance API client (live trading)
pybit>=5.6.0                # Bybit API client (live trading, v5)
python-telegram-bot>=20.0   # Telegram alerts
aiohttp>=3.9.0              # Async HTTP; paper-trading live price fetch + Bybit/Binance WS
